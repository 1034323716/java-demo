package leet_code;

public class Solution {
    public String customSortString(String S, String T) {
        StringBuffer ss = new StringBuffer();
        StringBuffer tt = new StringBuffer();

        char[] aa = S.toCharArray();
        for (char a : aa) {
            boolean b = T.indexOf(a) > -1;
            if (b) {
                //T中
            }
        }

        return null;
    }
}
